import styled from 'styled-components'

const Wrap = styled.button`
    margin: 24px auto 0;
    border: none;
    outline: none;
    cursor: pointer;
    min-width: 296px;
    height: 56px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(32.95deg, var(--gradient-start) 8.75%, var( --gradient-end) 86.64%);

    font-style: normal;
    font-weight: 300;
    font-size: 18px;
    line-height: 26px;
    color: var(--color-light);
`;

export default Wrap